# Hugging Face Spaces Dockerfile (port 7860)
# This file is used ONLY for HF deployment. The original Dockerfile
# remains unchanged for local Docker / Minikube / Helm deployments.

# ── Stage 1: Build dependencies ───────────────────────────────────
FROM python:3.13-slim AS builder
WORKDIR /app

# Install build dependencies for psycopg2 and cryptography
RUN apt-get update && \
    apt-get install -y --no-install-recommends gcc libpq-dev && \
    rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir --prefix=/install -r requirements.txt

# ── Stage 2: Production runner ────────────────────────────────────
FROM python:3.13-slim AS runner
WORKDIR /app

# Runtime dependency for psycopg2
RUN apt-get update && \
    apt-get install -y --no-install-recommends libpq5 && \
    rm -rf /var/lib/apt/lists/*

# Copy installed packages from builder
COPY --from=builder /install /usr/local

# Copy application code
COPY app/ ./app/

# Create non-root user (HF Spaces requirement)
RUN addgroup --system --gid 1001 appgroup && \
    adduser --system --uid 1001 --ingroup appgroup appuser && \
    chown -R appuser:appgroup /app

USER appuser

# HF Spaces requires port 7860
EXPOSE 7860

# Environment variables (set via HF Space Secrets)
# DATABASE_URL, BETTER_AUTH_SECRET, BETTER_AUTH_URL,
# OPENROUTER_API_KEY, MODEL_NAME

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "7860"]
